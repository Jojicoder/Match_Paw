name: Build Windows Installer

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Install WiX Toolset
        run: choco install wixtoolset -y

      - name: Build runtime image (jlink)
        working-directory: Match_paw_desktop
        run: mvn clean javafx:jlink

      - name: Create app image (jpackage phase 1)
        working-directory: Match_paw_desktop
        run: jpackage --type app-image --name MatchPaw --module com.matchpaw/com.matchpaw.App --runtime-image target/matchpaw-runtime --dest target

      - name: Create Windows installer (jpackage phase 2)
        working-directory: Match_paw_desktop
        run: jpackage --type exe --app-image target/MatchPaw --dest target/installer --name MatchPaw --app-version 1.0.0 --vendor MatchPaw --win-dir-chooser --win-menu --win-shortcut --icon src/main/resources/icon.ico

      - name: Upload Installer
        uses: actions/upload-artifact@v4
        with:
          name: MatchPaw-Windows-Installer
          path: Match_paw_desktop/target/installer/*.exe
